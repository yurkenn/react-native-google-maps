import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    backgroundColor: 'white',
    top: 450,
    alignSelf: 'center',
    padding: 10,
    borderRadius: 20,
    borderColor: '#e0e0e0',
  },
});
